---
interface Card {
  title: string;
  text: string;
}

interface Props {
  cards: Card[];
}

const { cards } = Astro.props;
---

<div class="card-grid">
  {cards.map((card) => (
    <article class="card">
      <h3>{card.title}</h3>
      <p>{card.text}</p>
    </article>
  ))}
</div>